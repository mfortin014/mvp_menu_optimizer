  # .envrc — committed, no secrets
  if [ -z "${BWS_ACCESS_TOKEN:-}" ]; then
    echo "Set BWS_ACCESS_TOKEN (Bitwarden Secrets Manager machine token) before entering this dir."
    exit 1
  fi

  PROJECT_ID="4b5e71e8-c979-4125-acba-b37101280d71"  # from BWS UI or: bws project list
  eval "$(bws secret list "$PROJECT_ID" --output env)"
