{
  "name": "menu-optimizer-goc",
  "private": true,
  "type": "module",
  "scripts": {
    "build:goc": "tsc -p tsconfig.goc.json",
    "clean:goc": "rimraf dist/goc || rm -rf dist/goc",
    "check:goc": "tsc -p tsconfig.goc.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
